from turtle import Screen

screen = Screen()
# Setup screen dimensions
screen.setup(width=600, height=600)
# Turn off the animation (tracer) for instant updates
screen.tracer(0)
# Listen for keyboard events
screen.listen()

# Additional settings can be added here, such as background color:
# screen.bgcolor("white")
